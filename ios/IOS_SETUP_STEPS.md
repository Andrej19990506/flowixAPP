# 📱 Настройка iOS QR Scanner - Пошаговая инструкция

## ✅ Что уже готово:
- Все файлы созданы в `ios/FlowixApp/QrScanner/`
- JS код обновлен и готов
- Info.plist уже содержит разрешение на камеру

## 🚀 Шаги для интеграции:

### Шаг 1: Открыть проект в Xcode

```bash
cd ios
open FlowixApp.xcodeproj
```

Или просто дважды кликнуть на `ios/FlowixApp.xcodeproj` в Finder.

---

### Шаг 2: Добавить файлы в проект

1. **В Xcode** слева в навигаторе (Project Navigator) найдите папку `FlowixApp`
2. **Правый клик** на папку `FlowixApp` → **"Add Files to FlowixApp..."**
3. Выберите папку `ios/FlowixApp/QrScanner/`
4. ✅ **ВАЖНО:** Отметьте галочки:
   - ☑️ "Copy items if needed" (если папка не видна)
   - ☑️ "Create groups" (не folder references!)
   - ☑️ Target: **FlowixApp** должен быть выбран
5. Нажмите **"Add"**

**Результат:** Папка `QrScanner` должна появиться в проекте с тремя файлами:
- `QrScannerNativeModule.swift`
- `QrScannerViewController.swift`
- `QrScannerNativeModule.m`

---

### Шаг 3: Проверить что файлы в Target

1. Выберите файл `QrScannerNativeModule.swift` в навигаторе
2. Справа в **File Inspector** (справа панель) найдите **"Target Membership"**
3. Убедитесь что галочка стоит на **FlowixApp** ✅

Повторите для всех трех файлов.

---

### Шаг 4: Создать Bridging Header (если нужно)

Если Xcode попросит создать Bridging Header:

1. Xcode предложит создать `FlowixApp-Bridging-Header.h` - нажмите **"Create Bridging Header"**
2. Или создайте вручную:
   - Правый клик на `FlowixApp` → **New File...**
   - **Header File** → `FlowixApp-Bridging-Header.h`
3. Добавьте в этот файл:

```objective-c
#import <React/RCTBridgeModule.h>
#import <React/RCTEventEmitter.h>
#import <UIKit/UIKit.h>
#import <AVFoundation/AVFoundation.h>
#import <Vision/Vision.h>
```

4. В настройках проекта:
   - Выберите **FlowixApp** в навигаторе (самый верхний элемент)
   - Выберите **FlowixApp** target
   - Вкладка **Build Settings**
   - Найдите **"Objective-C Bridging Header"**
   - Укажите путь: `FlowixApp/FlowixApp-Bridging-Header.h`

---

### Шаг 5: Установить зависимости

```bash
cd ios
pod install
cd ..
```

---

### Шаг 6: Собрать и запустить

#### Вариант А: Через Xcode
1. Выберите симулятор или устройство (iPhone)
2. Нажмите ▶️ **Run** (Cmd+R)

#### Вариант Б: Через терминал
```bash
npm run ios
```

---

## 🔍 Проверка что всё работает:

1. Откройте приложение
2. Перейдите в Inventory
3. Нажмите кнопку сканирования QR
4. Должен открыться нативный экран камеры с:
   - Кнопкой закрытия (X) вверху справа
   - Кнопкой фонарика внизу
   - Прозрачной областью в центре для сканирования

---

## ⚠️ Возможные проблемы:

### Ошибка: "No such module 'React'"
**Решение:** 
```bash
cd ios
pod install
cd ..
```
И пересоберите проект.

### Ошибка: "Cannot find type 'RCTEventEmitter'"
**Решение:** Проверьте Bridging Header (Шаг 4).

### Ошибка: Файлы не компилируются
**Решение:** Убедитесь что все файлы добавлены в Target Membership (Шаг 3).

### Симулятор не запускается
**Решение:** 
- В Xcode: **Xcode** → **Settings** → **Platforms** → скачать iOS Simulator
- Или выберите реальное устройство (iPhone)

---

## ✅ После успешной сборки:

QR Scanner будет работать на iOS точно так же как на Android:
- ✅ Открытие нативного экрана камеры
- ✅ Распознавание QR и DataMatrix кодов
- ✅ Определение дубликатов
- ✅ Фонарик
- ✅ Вибрация при сканировании

---

**Готово!** 🎉

